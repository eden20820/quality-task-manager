import{MessagesCenter,type EmailLogRow}from"@/components/messages/messages-center";import{PaginationControls}from"@/components/pagination-controls";import{PAGE_SIZE,pageRange,parsePage}from"@/lib/pagination";import{createClient}from"@/lib/supabase/server";
export default async function MessagesPage({searchParams}:{searchParams:Promise<{page?:string}>}){const page=parsePage((await searchParams).page),{from,to}=pageRange(page),s=await createClient();const{data,count,error}=await s.from("email_delivery_log").select("id,notification_type,recipient_name,recipient_email,subject,status,error_message,created_at",{count:"exact"}).order("created_at",{ascending:false}).range(from,to);if(error)console.error(error);return <div className="space-y-5"><MessagesCenter rows={(data??[]) as EmailLogRow[]}/><PaginationControls basePath="/messages" page={page} pageSize={PAGE_SIZE} total={count??0}/></div>}
